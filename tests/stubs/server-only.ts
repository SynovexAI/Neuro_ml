// Test stub for the `server-only` package (which throws if imported outside an
// RSC server context). Aliased in vitest.config.ts so pure server-lib logic
// can be unit-tested in a plain Node environment.
export {};
