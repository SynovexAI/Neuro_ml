# ML Final Submission

The ML module includes dataset upload/compressed CSV handling, preprocessing, feature engineering, classification, regression, clustering, model comparison, hyperparameter tuning, cross-validation, explainability, drift detection, model versioning and export.

The lifecycle-version metrics typing was corrected in MlLab.tsx so classification and regression metrics are built as a concrete Record<string, number>.
